#include <math.h>
#include <string.h>

int is_palindrom(char text[]){
    char texting[40];
    int count = 0;
    while (text[count] != '\0'){
        if (isalpha(text[count]) != 0){
            if (isupper(text[count]) != 0) tolower(text[count]);
            
        }
    }
}
